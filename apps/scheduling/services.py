"""Phase >=1 scheduling service entrypoints."""

from typing import NoReturn


def create_appointment() -> NoReturn:
    """Create an appointment when the scheduling domain is implemented."""
    message = "Phase >=1"
    raise NotImplementedError(message)
